export const OPTIONAL_HOST_PERMISSIONS = ['https://*/*', 'http://*/*'] as const;

export const LOCALHOST_DEBUG_ORIGINS = ['http://localhost/*'] as const;

export const OPTIONAL_HOST_PERMISSION_LIST = [...OPTIONAL_HOST_PERMISSIONS];
